---
title: "System Variables"
description: "Nodes that set the built-in sys_ variables: Date, DateTime, Time, UUID, and Random Number."
---

## Date

Insert a date. Use the offset to select a day before or after by number of days. The date will also be available via the variable "sys_Date"

:::note[Parameters]

- **timezone**: The timezone

- **offset**: The number of days before or after the current day
:::

---

## DateTime

Insert a dateTime. Use the offset to select a day before or after by number of days. The date will also be available via the variable "sys_Date"

:::note[Parameters]

- **timezone**: The timezone

- **offset**: The number of days before or after the current day
:::

---

## Time

Insert a time. Use the offset to select a time before or after by number of hours. The time will also be available via the variable "sys_Time"

:::note[Parameters]

- **timezone**: The timezone

- **offset**: The number of hours before or after the current hour
:::

---

## UUID

Create a UUID. The UUID will also be available via the variable "sys_UUID"

---

## Random Number

Generate a random number. The number will also be available via the variable "sys_Random"

:::note[Parameters]

- **upper**: The max number

- **lower**: The Min Number
:::
