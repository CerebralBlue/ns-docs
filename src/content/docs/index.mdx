---
title: NeuralSeek Documentation
description: Official documentation for NeuralSeek — guides, reference, and how-tos.
template: splash # Remove or comment out this line to display the site sidebar on this page.
hero:
  tagline: Everything you need to build, configure, and ship with NeuralSeek.
  image:
    # Raw <img> instead of `file:` on purpose: Starlight's Hero hardcodes
    # width/height 400 with no srcset, so a `file:` hero is always emitted at
    # 400px and looks soft once scaled up. Serving the 1200px asset directly
    # lets the browser downsample and stay crisp on hi-DPI screens.
    # alt is intentionally empty: the art is decorative, and a non-empty alt
    # paints as text inside the reserved box while the image loads.
    # NOTE: /ns-docs prefix = GitHub Pages base path; drop it when the custom
    # domain lands (base becomes '/'). See AGENTS.md.
    html: '<img src="/ns-docs/hero-folder-ns.webp" width="1200" height="1200" alt="" loading="eager" decoding="async" fetchpriority="high" />'
  actions:
    # NOTE: Starlight does NOT auto-prefix the base path to hero action links
    # (unlike nav/sidebar links). /ns-docs = GitHub Pages base; drop it when the
    # custom domain lands (base → '/'). See AGENTS.md.
    - text: Get started
      link: /ns-docs/getting-started/quickstart-seek/
      icon: right-arrow
    # TODO: secondary action becomes "Try the live demo" once the team picks
    # the target (chat widget / labs / demo instance) — open question for RJ.
    - text: NeuralSeek.com
      link: https://neuralseek.com
      icon: external
      variant: minimal
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

## Explore the docs

<CardGrid stagger>
	<Card title="Set up NeuralSeek" icon="rocket">
		Deploy an instance and get your first grounded answer —
		[start here](/ns-docs/getting-started/quickstart-seek/).
	</Card>
	<Card title="Answer questions with Seek" icon="magnifier">
		Tuning, virtual KBs, filters, caching, and curation —
		[Seek documentation](/ns-docs/seek/overview/).
	</Card>
	<Card title="Build agents with mAIstro" icon="puzzle">
		Agent workflows and the full NTL node reference —
		[mAIstro documentation](/ns-docs/maistro/overview/).
	</Card>
	<Card title="Connect your stack" icon="setting">
		Knowledge bases, LLMs, virtual agents, and platform integrations —
		[integrations](/ns-docs/knowledge/connect-a-kb/).
	</Card>
</CardGrid>

## Ask the docs

These docs come with their own AI assistant — fitting, since answering questions
over a knowledge base is exactly what NeuralSeek does.

Hit **Ask AI** in the corner and ask in plain language: _"How do I connect a
Pinecone index?"_ or _"Which LLMs support multimodal input?"_ It reads across
every page here and links you to the source so you can verify the answer —
faster than scrolling the sidebar when you know what you want but not where it
lives.
