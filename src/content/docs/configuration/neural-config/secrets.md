---
title: "Secrets"
description: "The instance secret store that agent parameters fall back to when `prompt: false` is set."
---

:::note[Draft]
This page is part of the new documentation structure and its content is being prepared.
:::

This page is brand new for the restructured docs.

## To document on this page

- Secrets behave differently on on-prem installs
- `prompt: false` parameters fall back to the secret store — pass the parameter explicitly to override

